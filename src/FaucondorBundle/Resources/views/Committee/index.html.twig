{% extends 'FaucondorBundle::layout.html.twig' %}

{% block content -%}
    <h1>{{ 'entity.committee.title'|trans }}</h1>
    <div class="tables">
        <table class="table records_list datatable">
            <thead>
                <tr>
                    <th>{{ 'entity.committee.name'|trans }}</th>
                    <th>{{ 'entity.committee.chair'|trans }}</th>
                    <th>{{ 'entity.committee.createdat'|trans }}</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('committee_edit', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                    <td>{{ entity.chair }}</td>
                    <td>{% if entity.createdat %}{{ entity.createdat|date('Y-m-d H:i:s') }}{% endif %}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
