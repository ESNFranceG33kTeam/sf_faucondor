{% extends 'FaucondorBundle::layout.html.twig' %}

{% block content -%}
<section>
    <h1>Events edit</h1>

    <div>
        {{ form_start(edit_form) }}

        {{ form_row(edit_form.name) }}<br>
        {{ form_row(edit_form.start) }}<br>
        {{ form_row(edit_form.end) }}<br>

        <input type="submit" value="{{ 'label.button.save'|trans }}" class="button" />

        {{ form_end(edit_form) }}
    </div>
</section>
{% endblock %}
